import { InputWithIconDirective } from './input-with-icon.directive';

describe('InputWithIconDirective', () => {
  it('should create an instance', () => {
    const directive = new InputWithIconDirective();
    expect(directive).toBeTruthy();
  });
});
